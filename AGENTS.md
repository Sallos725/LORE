# LORE — Project Agent Instructions

## 프로젝트 목적

LORE는 PocketRisu에서 사용하는 **위키 기반 장기 서사 기억 플러그인 + 서버 사이드카** 프로젝트다.

사용자는 RisuBard의 위키·장기기억 기능을 원하지만 iPhone Safari에서 OOM을 경험하고 있다. PocketRisu의 가벼운 사용 경험을 유지하면서, 기억 정리·저장·검색·컨텍스트 조립을 서버로 옮기는 것이 목표다.

- 브라우저는 설정, 상태 표시, 위키 검색·열람·수정에 집중한다.
- 서버는 원문 근거, 위키 상태, 변경 이력, 검색, LLM 작업과 작업 복구를 소유한다.
- 좋은 기억 품질과 Safari 메모리 안정성을 각각 검증한다. 사이드카를 붙였다는 이유만으로 OOM 해결을 주장하지 않는다.
- 사용자 지시(2026-09-21): PocketRisu를 수정하거나 재빌드하지 않는다. 설치 가능한 Lite 플러그인 또는 Full 플러그인 + HTTP(S) 사이드카로 제공한다. 두 판 모두 사용자가 기억 추출 공급자·주소·모델·키를 별도로 선택한다.

## 현재 상태와 작업 범위

2026-09-22에 0.1.0-alpha.4를 GitHub Actions로 발행했다. latest도 같은 amd64/arm64 이미지이며 공개 파일 6개를 다운로드해 체크섬과 로컬 빌드 일치를 검증했다. 별도 scope token을 제거하고 기존 PocketRisu 로그인으로 Full을 인증한다. 저장된 chaId/chat.id별로 위키·원문·이력·작업·검색·주입을 분리하고 채팅 전환을 따라간다. Lite는 자동 노트북·전체 백업/복원, Full은 SQLite·영속 추출 설정/작업을 제공한다. 공통 beforeRequest에서 현재 질문과 최근 대화로 관련 기억을 검색해 예산 안에서 주입한다. 선택적 OpenAI 최종 body 검사를 유지한다. 큐 정체·늦은 주입 상태·숫자 인덱스 호환·HTTP 저장 재전송을 수정했다. 실제 원본 호스트·합성 LLM 검증 근거와 원격 배포 상태는 docs/status.md, docs/public-readiness.md를 확인한다. iPhone 실기기 OOM은 미검증이며 공개 테스트 버전이다. Gitea Actions 실패 조사는 사용자 지시에 따라 보류한다.

- LORE를 독립 프로젝트로 개발한다. 기본 작업 범위는 이 디렉터리다.
- 상위 작업 공간의 오래된 NodeOnly 프로토타입 설명을 현재 PocketRisu API의 증거로 사용하지 않는다. 실제 대상 버전과 소스를 확인한다.
- 초기 구현은 JavaScript ES modules, Node.js 22.23 이상 내장 HTTP/SQLite를 사용한다. 런타임 외부 npm 의존성은 없으며 선택 이유는 `docs/design.md`에 기록한다.
- 사용자 요청(2026-09-21)에 따라 Lite는 브라우저 단독 플러그인, Full은 얇은 플러그인과 서버 사이드카로 제공한다. Lite는 제한된 데이터와 작업만 처리하며 탭 종료 후 작업 지속이나 Safari OOM 해결을 보장하지 않는다.
- 범용 플러그인 매니저와 별도 채팅 앱은 초기 범위가 아니다.
- 기존 서비스의 데이터를 자동 이전하거나 실행 중인 스택을 교체하는 것은 초기 구현에 포함하지 않는다.

## 참고 프로젝트

- PocketRisu: https://github.com/PocketRisu/PocketRisu
- RisuBard: https://github.com/rpaddict/RisuBard
- RisuBard UI 독립 코어 후보: `packages/risubard-core/`
- PocketRisu 플러그인 저장소 문서: `docs/en/plugin-storage.md`
- 로컬 참고 구현: `../MARP/`, `../VEIL/`, `../ArchiveCenter/`

재사용 전에 실제 파일, 의존성, 라이선스와 대상 커밋을 확인한다. 코어 디렉터리가 분리되어 있어도 BardWiki 전체가 독립 패키지로 실행된다고 가정하지 않는다. 코드를 가져오면 출처·버전·필요한 고지를 함께 보존한다.

MARP·VEIL은 얇은 플러그인과 사이드카 연결의 참고 사례다. ArchiveCenter는 원문·파생 기억 저장과 검색의 연동 후보다. LORE는 이들 없이도 동작하고, 연동은 선택 사항으로 둔다.

## 목표 구조

```text
iPhone Safari / 기존 PocketRisu + LORE 플러그인
  Lite -> 기존 PocketRisu HTTP 원문 조회(1 MiB/128개 이하)
       -> 기기 저장·위키·선택한 LLM 직접 호출
  Full -> HTTP(S) 설정·수집 요청·상태·위키·기억 조회
       -> LORE 사이드카 -> 설정된 기존 PocketRisu HTTP API
                        -> SQLite 원문·위키·영속 추출 큐
                        -> 선택한 로컬/원격 LLM 직접 호출
```

기존 beforeRequest의 확정 원문 ID와 저장된 chat/message ID를 대조한다. Full의 원문은 서버에서만 읽는다. 생성 중 출력·준비 프롬프트·분석 결과를 원문처럼 저장하지 않는다. 다음 요청 전에는 새 답변을 수집하지 못하며 탭 종료 후 완전한 새 이벤트 수집을 보장하지 않는다. 서버가 수락한 작업만 탭 종료 뒤에도 계속한다.

호스트 저장 outbox나 인증 프록시는 현재 지원 기능이나 필수 설치 단계가 아니다. 기존 API만으로 구현할 수 없는 보장은 명시적으로 제외하고, 앱 변경을 요구하는 기능은 사용자 요청 없이 다시 도입하지 않는다.

## 기억 모델의 필수 원칙

1. **원문과 파생 기억을 구분한다.** 원문은 근거이며 위키는 수정·재생성 가능한 파생 상태다. 원문 보존 책임이 PocketRisu인지 LORE인지 명시하고, 근거 조회 경로를 유지한다.
2. **정체성은 안정된 ID로 관리한다.** 설치/사용자 범위, 캐릭터, 채팅, 분기, 메시지 ID와 revision을 구분한다. 표시 이름이나 배열 인덱스만으로 기억을 연결하지 않는다.
3. **확정된 대화만 사실로 반영한다.** 스트리밍 중간 출력, 버린 재생성 후보, MARP의 분석·미래 전개 제안은 확정 사건이 아니다. 확정 시점을 호스트 동작과 맞춰 정의한다.
4. **변경된 사실과 과거 사실을 구분한다.** 관계·위치·소유물·목표 등의 현재 상태와 변화 이력을 보존한다. 모순된 내용을 무조건 덮어쓰지 않는다.
5. **근거를 추적한다.** 기억 항목에 출처 메시지 ID/revision, 관련 분기, 변경 이력을 연결한다. 원문에서 확인되지 않은 추론은 확정 사실과 구분한다.
6. **수정·삭제·재생성·분기를 처리한다.** 무효화된 메시지에서 파생된 기억을 제외하거나 재계산한다. 다른 분기 및 되돌리기 이전의 미래 사건이 현재 컨텍스트에 섞이면 안 된다.
7. **인물별 지식을 구분한다.** 세계의 사실과 특정 인물이 아는 사실은 다르다. 비밀·공개 범위를 저장하고 검색 결과에도 적용할 수 있도록 설계한다.
8. **사용자 교정을 존중한다.** 수동 수정·고정한 내용은 자동 갱신이 조용히 덮어쓰지 않는다. 충돌을 표시하고 근거·되돌리기를 제공한다.

위키는 인물·관계·장소·사건·현재 장면·미해결 약속을 표현할 수 있어야 한다. 첫 버전은 인물·사건·현재 장면부터 시작한다.

## 서버 작업과 컨텍스트

- 갱신 작업은 영속 큐에서 처리한다. queued/running/completed/failed/cancelled 상태, 타임아웃, 제한된 재시도와 재시작 복구를 정의한다.
- 이벤트 ID 및 메시지 revision으로 멱등성을 보장한다. 중복 전달, 순서 역전, 늦게 완료된 작업이 최신 기억을 훼손하지 않도록 한다.
- 같은 채팅·분기의 갱신은 순서 또는 revision 검증으로 보호한다. LLM 호출 중 잠금을 장시간 유지하지 않는다.
- LLM 출력은 검증된 변경 제안으로 취급한다. 스키마와 근거를 검증한 뒤 원자적으로 반영한다.
- 위키 갱신을 매 답변의 긴 동기 대기로 만들지 않는다. 처리 완료 지점과 아직 반영되지 않은 최근 대화를 구분한다.
- 생성 직전에는 관련 기억만 조회한다. 초기에는 단순 검색으로 시작할 수 있으며 임베딩·벡터 DB·reranker를 필수로 만들지 않는다.
- 기억 토큰 예산과 전체 프롬프트 예산을 구분한다. 응답 예약량, 로어북, MARP·ArchiveCenter 주입과 함께 지원 요청의 전체 예산을 검사한다. 현재 기존 API에 tokenizer가 없으므로 보수적 UTF-8 추정임을 표시하고 정확한 token count를 보장하지 않는다.
- 필수 컨텍스트 초과를 조용히 잘라내지 않는다. 포함·제외한 기억과 그 이유를 확인할 수 있게 한다.
- 사이드카 장애 시 일반 채팅을 계속할 수 있는 동작을 기본으로 설계하고 기억 누락/지연을 표시한다. 오래된 기억을 최신 상태처럼 표시하지 않는다.

## Safari 메모리 원칙

- 전체 DB·전체 위키를 매 턴 복사하지 않는다. Full 원문은 서버에서만 읽는다. Lite는 기존 API의 제약상 선택 채팅을 읽되 응답 1 MiB·메시지 128개 상한과 스트림 취소를 유지한다.
- `getDatabase()` 및 `getCharacter()` 등은 이름만 보고 가벼운 API라고 가정하지 않는다. 반환 크기와 iframe/RPC 복사 비용을 확인한다.
- 플러그인 저장소에는 설정과 작은 식별자만 둔다. 전체 위키를 한 키의 거대한 JSON으로 저장하지 않는다.
- V2 전체 저장소 사전 로드나 V3의 전체 플러그인 데이터 제공 옵션에 의존하지 않는다. 필요한 키만 비동기로 읽는다.
- 채팅 전달은 신규/변경 메시지 중심으로 한다. Full 과거 기록은 서버에서 읽고 delta 단위로 처리한다. 기존 호스트 HTTP API 자체가 페이지 조회인 것처럼 표현하지 않는다.
- 위키 목록은 페이지 단위로, 본문은 선택한 문서만 읽는다. 편집기와 무거운 UI는 필요할 때 로드한다.
- 캐시, 로그, 작업 상태 목록과 요청/응답 크기에 상한을 둔다. 닫힌 UI의 이벤트 리스너·타이머·요청을 정리한다.
- Markdown/HTML 렌더링 시 저장된 콘텐츠를 신뢰하지 않고 안전하게 처리한다.

## 저장 및 API 설계

초기 후보는 단일 사이드카와 경량 영구 저장소다. Markdown 위키 열람·내보내기를 지원하되, Markdown과 DB 중 무엇이 정본인지 먼저 정한다. 검색 인덱스는 재구축할 수 있어야 한다.

아래는 API 책임의 예시이며 확정 경로가 아니다.

```http
GET  /health
POST /events                   # 확정 메시지 및 수정·삭제·분기 이벤트
GET  /jobs/:jobId               # 재연결 후 조회 가능한 작업 상태
POST /context                  # 범위·revision·예산에 맞는 기억 조회
GET  /wiki                     # 범위 지정 검색 및 페이지 조회
GET  /wiki/:pageId              # 개별 문서와 근거
PATCH /wiki/:pageId             # revision 검증을 포함한 수동 교정
```

- 서버가 인증된 사용자/채팅 범위를 검증한다. 클라이언트가 보낸 ID만으로 다른 범위의 기억에 접근하게 하지 않는다.
- 쓰기는 호스트의 활성 세션 정책을 따른다. 서버 내부 작업은 검증된 이벤트 범위에서 실행한다.
- 사이드카 기본 포트는 Docker 내부에 둔다. 제공한 HTTP Compose override로 지정한 내부망 인터페이스에 공개할 수 있다. 플러그인은 HTTP(S)와 기존 PocketRisu 로그인으로 연결한다. 별도 LORE 토큰을 만들거나 입력하게 하지 않는다.
- 프록시를 추가할 수 없는 초기 배포는 별도 연결 방식을 명시한다. 임의 URL 프록시나 무인증 공개 포트를 기본값으로 두지 않는다.
- 요청 크기, 동시 작업 수, LLM 호출 시간·출력 크기를 제한한다. 로그에 API 키와 대화 원문 전체를 기본 출력하지 않는다.
- Docker socket이나 광범위한 호스트 경로를 마운트하지 않는다. LORE 데이터 볼륨과 필요한 설정만 사용한다.
- 임의 사용자 코드를 실행하는 플러그인 런타임은 만들지 않는다.

## 권장 구현 순서

1. **호환성 조사:** 대상 PocketRisu 버전, 요청 훅, 저장 확정 시점, 메시지 ID, 수정·분기 이벤트와 서버 연결점을 확인하고 기록한다. RisuBard 코어 재사용 여부도 판단한다.
2. **독립 기억 엔진:** 합성 대화로 원문 근거, 인물·사건·장면 갱신, 검색, 토큰 예산과 변경 이력을 검증한다.
3. **사이드카:** health, 영구 저장, 작업 큐, 이벤트 수신, context 및 페이지 단위 wiki API를 구현한다. Docker 단독 실행을 제공한다.
4. **얇은 플러그인:** 연결 설정, 상태, 검색·열람·수정, 제한된 기억 주입을 구현한다. 서버 연결이 없는 단계의 한계를 문서화한다.
5. **서버 연결:** 확정 이벤트의 신뢰성 있는 전달, 생성 전 기억 조회, 탭 종료 후 처리와 재연결 복구를 구현한다.
6. **모바일 검증:** 동일한 데이터로 PocketRisu 단독과 LORE 활성 상태를 비교한다. 필요하면 그 후에 ArchiveCenter·MARP·VEIL 연동을 추가한다.

## 검증과 완료 기준

변경 범위에 맞는 테스트를 실행한다. 초기부터 다음 동작은 의미 있는 자동 검증 대상으로 삼는다.

- 중복 이벤트 재전송이 기억을 중복 생성하지 않는다.
- 메시지 수정·삭제·재생성 및 분기 후 잘못된 기억이 검색되지 않는다.
- 과거 revision의 늦은 작업이 최신 상태와 수동 교정을 덮어쓰지 않는다.
- 검색 범위가 사용자·캐릭터·채팅·분기 및 공개 범위를 넘지 않는다.
- 컨텍스트가 지정 예산을 지키며 근거를 추적할 수 있다.
- LLM 실패·잘못된 출력·타임아웃으로 저장 상태가 부분 손상되지 않는다.
- 서버 연결 단계에서는 클라이언트 종료와 서비스 재시작 후 작업이 복구된다.
- 사이드카 비활성/장애 시 일반 채팅 동작과 사용자 상태 표시를 확인한다.

실기기에서는 긴 채팅, 큰 위키, 이미지 포함 대화, 반복 UI 열기/닫기, 화면 잠금·재접속을 점검한다. 앱 버전·기기·데이터 규모·활성 플러그인·측정 가능한 메모리/전송량을 기록한다. 데스크톱 테스트만으로 Safari OOM 해결을 선언하지 않는다.

## 작업 규칙 및 인수인계

- 작은 변경 단위로 진행하고 테스트 가능한 상태를 유지한다.
- 작은 변경마다 LORE 독립 저장소에 커밋한다. GitHub `Sallos725/lore`와 Gitea `M1NDB3ND3R/lore` Actions로 Lite/Full 플러그인, 서버 패키지와 컨테이너를 배포한다. 버전 이미지를 보존하며 릴리스 성공 후 같은 amd64/arm64 index를 latest에도 연결한다. 현재 latest는 알파를 포함한다. 실제 원격 발행과 로컬 준비 완료는 구분한다.
- LORE 작업과 PocketRisu 앱 수정, 상위 Compose 변경은 분리한다. 외부 앱 저장소의 브랜치 규칙을 따르고 `Risuai-NodeOnly/` 수정 전에는 새 브랜치를 만든다.
- 기존 dirty/untracked 파일, 설정, 비밀 값과 사용자 데이터를 덮어쓰거나 커밋에 섞지 않는다.
- 런타임 구현 요청은 그 범위 안에서 자율적으로 완료한다. 이 문서만을 근거로 추가 승인 절차를 만들지 않는다.
- 새 외부 유료 서비스나 대규모 실제 대화의 LLM 일괄 처리를 테스트 기본값으로 삼지 않는다. 합성 데이터로 검증한다.
- README에는 설치, 구성, 실제 지원 버전, 백업·복구, 알려진 제약을 기록한다. 설계 제안과 구현 완료를 명확히 구분한다.
- 주요 설계 선택과 미해결 사항은 프로젝트 문서에 남겨 다음 작업이 대화 기록 없이 이어질 수 있게 한다.
